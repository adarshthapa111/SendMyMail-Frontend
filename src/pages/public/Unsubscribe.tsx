import { PublicPlaceholder } from '../_shared/PublicPlaceholder';

export function Unsubscribe() {
  return (
    <PublicPlaceholder
      title="You've been unsubscribed"
      subtitle="Public — token in URL, no auth. Compliance: CAN-SPAM + Nepal Advertisement Act 2076."
      mockup="unsubscribe.html"
    />
  );
}
