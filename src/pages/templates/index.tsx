import { Placeholder } from '../_shared/Placeholder';

export function Templates() {
  return <Placeholder title="Templates" subtitle="Reusable email templates per client." mockup="templates.html" />;
}

/* Builder lives in ./Builder.tsx (its own lazy chunk because it's heavy). */
