import { Placeholder } from '../_shared/Placeholder';

export function Flows() {
  return <Placeholder title="Flows" subtitle="Triggered automations (welcome, abandoned cart, win-back)." mockup="flows.html" />;
}

export function FlowConfig() {
  return <Placeholder title="Flow config" subtitle="Trigger → wait → send → branch." mockup="flow_config.html" />;
}
